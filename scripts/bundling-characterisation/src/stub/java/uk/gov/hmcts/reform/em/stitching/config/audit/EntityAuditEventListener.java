package uk.gov.hmcts.reform.em.stitching.config.audit;

/**
 * Compile-time stub. The real em-stitching class drags in the service's JPA repositories and
 * Spring Security; the domain entities we compile only reference it from an
 * {@code @EntityListeners} annotation, which the characterisation harness never evaluates
 * (no JPA runtime is present).
 */
public class EntityAuditEventListener {
}
