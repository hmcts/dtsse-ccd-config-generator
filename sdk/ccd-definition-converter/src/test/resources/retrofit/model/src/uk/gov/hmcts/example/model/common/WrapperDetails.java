package uk.gov.hmcts.example.model.common;

/**
 * The payload complex type wrapped by {@link Wrapper}. The definition's {@code Wrapper}
 * {@code ComplexTypes} rows describe THIS class's members, because CCD roots a collection element's
 * member namespace at the element's {@code value} type.
 */
public class WrapperDetails {

  private String existingMember;
}
