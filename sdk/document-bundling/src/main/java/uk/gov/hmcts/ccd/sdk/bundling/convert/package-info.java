/**
 * The built-in per-media-type document handlers: PDF passthrough, image conversion,
 * Docmosis-backed office conversion, and the generated media link page. The effective registry
 * itself lives in the api package ({@code HandlerRegistry}); this package supplies the default
 * registrations the {@code BundleRendererBuilder} seeds it with.
 */
package uk.gov.hmcts.ccd.sdk.bundling.convert;
