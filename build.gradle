
subprojects {
    apply plugin: 'maven-publish'
    group = "ccd-sdk"
    version = '0.1.1'
    publishing {
        repositories {
            maven {
                url = "${project.rootDir}/repository"
                name = "repo"
            }
        }
    }
}
