name: SPTribs Check

on:
  push:
  pull_request:

jobs:
  sptribs-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: recursive
      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Run sptribs:check
        run: ./gradlew sptribs:check -i

  sptribs-semantic-diff:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout base branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: base
          fetch-depth: 0
          submodules: recursive
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          path: pr
          fetch-depth: 0
          submodules: recursive
      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Generate base CCD config
        working-directory: base
        run: ./gradlew sptribs:generateCCDConfig -i
      - name: Generate PR CCD config
        working-directory: pr
        run: ./gradlew sptribs:generateCCDConfig -i
      - name: Compare CCD configuration outputs
        working-directory: pr
        run: |
          python scripts/ccd_diff.py \
            ../base/test-projects/sptribs-case-api/build/definitions \
            test-projects/sptribs-case-api/build/definitions
